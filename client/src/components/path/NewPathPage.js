import React from 'react';
import NewPathForm from './NewPathForm';

const NewPathPage = (props) => {
  return (
    <section className="text-center">
      <h4>Add a new path</h4>
      <NewPathForm />
    </section>
  )
}

export default NewPathPage;
